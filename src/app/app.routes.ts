import { Routes } from '@angular/router';
import { AcquUserEntityComponent } from './acqu-user-entity/acqu-user-entity.component';

export const routes: Routes = [
    { path: '', component: AcquUserEntityComponent }
  ];